# pnpm-specific configuration
# node-linker=hoisted: Creates a flat node_modules structure (required for pnpm)
# Note: npm may show a warning about this unknown config, which is harmless.
# This project uses pnpm exclusively (see packageManager in package.json).
node-linker=hoisted
